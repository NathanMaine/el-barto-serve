# El Barto Serve - Configuration
# Copy to .env and adjust as needed.

# Path to Stable-DiffCoder model (local dir or HuggingFace model ID)
ELBARTO_MODEL_PATH=ByteDance-Seed/Stable-DiffCoder-8B-Instruct
# Pin to a specific model revision (commit hash) for reproducibility/security
# ELBARTO_MODEL_REVISION=

# Server bind address (0.0.0.0 = listen on all interfaces for network access)
ELBARTO_HOST=0.0.0.0
ELBARTO_PORT=8000

# API key for authentication (leave empty to disable auth)
# Clients send this as: Authorization: Bearer <key>
# ELBARTO_API_KEY=your-secret-key-here

# Diffusion generation defaults
# steps: More = higher quality, slower. 512 is max quality, 128 is fast.
ELBARTO_STEPS=256
# gen_length: Max output tokens per request
ELBARTO_GEN_LENGTH=512
# block_length: Diffusion block size (4 is the model default)
ELBARTO_BLOCK_LENGTH=4
# threshold: Early stopping confidence (0.0-1.0). Unset = use all steps.
#            Lower values = faster but potentially lower quality.
# ELBARTO_THRESHOLD=0.5
# remasking: Strategy for re-masking during denoising (low_confidence or random)
ELBARTO_REMASKING=low_confidence
